# Rust build artifacts
target/
# Tauri's generated schema + capability bindings (regenerated by build.rs)
gen/
# Per-target sidecar binaries copied in at package time (output of
# scripts/build_sidecar.sh, not checked in).
binaries/sidecar*
