fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2

# LLM SDKs. openai SDK also speaks to Dashscope's OpenAI-compatible endpoint
# (used for Qwen chat). `dashscope` is kept for Wanx image gen + CosyVoice TTS.
anthropic==0.34.2
openai==1.47.0
dashscope==1.20.7

beautifulsoup4==4.12.3
apscheduler==3.10.4

# mp3 duration probing for the renderer — tiny pure-Python dep.
mutagen==1.47.0

# OS-conventional user data / cache directories for the desktop build.
platformdirs==4.3.6
